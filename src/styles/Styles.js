const App =  {
  textAlign: "center"
};

const AppLogo = {
  animation: "App-logo-spin infinite 20s linear",
  height: "80px"
};

const AppHeader = {
  backgroundColor: "#222",
  height: "150px",
  padding: "20px",
  color: "white"
}

const AppTitle = {
  fontSize: "1.5em"
}

const AppIntro = {
  fontSize: "large"
}

const loginForm = {
  padding: "50px",
  margin: "0 auto",
  height: "500px",
  width: "500px",
  marginBottom: "100px"
}

const loginFormHeader = {
  color: "brown"
}

const todoHeader = {
  fontSize: "50px"
}

const renderTodoList = {
  margin: "0 ,auto",
  padding: "10px"
}

const centerAll = {
  textAlign: "center"
}

const addEmployeeForm = {
  margin: "0px 10px 10px 10px",
}

// const AppLogoSpin = @keyFrames AppLogoSpin {
//   from { transform: rotate(0deg); }
//   to { transform: rotate(360deg); }
// }

const accordion  = {
  backgroundColor: "#eee",
  color: "#444",
  cursor: "pointer",
  padding: "18px",
  width: "100%",
  border: "none",
  textAlign: "left",
  outline: "none",
  fontSize: "15px",
  transition: "0.4s",
}

// .active, .accordion:hover {
//   background-color: #ccc; 
// }

const panel = {
  padding: "0 18px",
  backgroundColor: "white"
}

const welcomeLogin = {
  float: "left",
  fontSize:"20px important;"
}

export default {
   App ,
   AppLogo ,
   AppHeader,
   AppTitle,
   AppIntro ,
   loginForm ,
   loginFormHeader,
   todoHeader ,
   renderTodoList ,
   centerAll ,
   addEmployeeForm ,
   accordion,
   panel,
   welcomeLogin
};
